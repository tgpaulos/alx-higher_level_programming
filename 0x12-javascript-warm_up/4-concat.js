#!/usr/bin/node

// Assuming the two arguments are passed as command line arguments
const arg1 = process.argv[2];
const arg2 = process.argv[3];

// prints the two arguments in the specified format
console.log(process.argv[2] + ' is ' + process.argv[3]);
